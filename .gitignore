# (intentionally empty on the dev-harness branch — see below)
#
# `main` ignores dev/ because GitHub Pages publishes every file in the repo, and a tracked
# dev/ would put a convincing fake job posting live on offeraio.com. This branch exists so
# the harness is still reachable by collaborators. It is NEVER merged to main.
